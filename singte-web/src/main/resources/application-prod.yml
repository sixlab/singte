server:
  port: 10000

spring:
  freemarker:
    cache: false
    settings:
      template_exception_handler: rethrow
  data:
    mongodb:
      uri: mongodb://localhost:27017/singte

logging:
  level:
    ROOT: info
    org.springframework.data.mongodb.core.MongoTemplate: debug
    cn.sixlab.minesoft.singte: debug

st:
  login:
    expire: 60
